import React from 'react';

const Price = () => {
    const prices = [
        "0"

    ]

    return (
        <React.Fragment>
            {prices}
        </React.Fragment>
    );
}

export default Price;